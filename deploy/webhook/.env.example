# Webhook 自动部署配置
# 复制此文件为 .env 并修改配置值
# cp deploy/webhook/.env.example deploy/webhook/.env
# chmod 600 deploy/webhook/.env

# GitHub Webhook Secret（在 GitHub 仓库 Settings → Webhooks 中配置）
WEBHOOK_SECRET=change_me_to_a_random_string

# Webhook 服务监听端口
WEBHOOK_PORT=9000
